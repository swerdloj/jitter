use super::ast;

/// Accepts a newly parsed AST and performs the following passes:
/// 1. Type-Checking & Type Validation
/// 2. TODO: ...
pub fn validate_ast(ast: ast::AST) {
    for node in ast {
        
    }
}